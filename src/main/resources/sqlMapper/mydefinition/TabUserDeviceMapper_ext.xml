<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lingtu.entity.mapper.TabUserDeviceMapper" >
 
 	<!-- 查询公司设备数量 -->
  <select id="selectCompanyCount" resultType="Integer" parameterType="String" >
   select count(a.COMPANYID)  from tab_user_device a,tab_user b
	where a.USERID=b.USERID and b.USERNAME=#{username}
  </select>

<!-- 查询公司id -->
  <select id="selectCompanyid" resultType="Integer" parameterType="String" >
   select distinct a.COMPANYID  from tab_user_device a,tab_user b
	where a.USERID=b.USERID and b.USERNAME=#{username}
  </select>
  
  <delete id="deleteByDeviceid" parameterType="java.lang.String" >
    delete from tab_user_device
    where DEVICEID = #{deviceid}
  </delete>
  <select id="select" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
select 
<include refid="Base_Column_List" />
    from tab_user_device where companyid = #{companyid,jdbcType=INTEGER}
</select>
<!--jiangmingjun 根据用户id更新设备id  -->
<update id="updateDeviceid" parameterType="com.lingtu.entity.TabUserDevice" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Dec 21 16:45:23 CST 2016.
    -->
    update tab_user_device
    <set >
      <if test="deviceid != null" >
        DEVICEID = #{deviceid,jdbcType=CHAR},
      </if>
    </set>
    where USERID = #{userid,jdbcType=BIGINT}
  </update>
 <!-- jiangmingjun根据用户查询是否有设备id -->
   <select id="selectDeviceid" resultType="String" parameterType="java.lang.Long" >
    select deviceid
    from tab_user_device
    where USERID = #{userid,jdbcType=BIGINT}
  </select>
</mapper>